language: python

python:
  - 3.7
  - 3.7.2
  - 3.8

services:
  - docker

before_script:
  - docker build -t filosof/parse:first .

script:
  - docker run --rm filosof/parse:first --param_search проверка